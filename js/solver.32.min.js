var g_fnMessagesCallbackSolver = [];

function solver_registerMessagesCallback(e) {
	g_fnMessagesCallbackSolver.push(e)
}

function solver_log(e, t, s) {
	for (var i = 0; i < g_fnMessagesCallbackSolver.length; i++) g_fnMessagesCallbackSolver[i](e, t, s)
}

function ifaceGetDefaultSection() {
	var e, t = g_Struct.getSectionIDs();
	return properties = new Properties, properties.transversalContractionCoef = .3, t.length ? (e = t[0], t = g_Struct.cache.getSection(e), properties.modulusE = Number(t.E), properties.calculateModulusG(), properties.area = Number(t.A), t.Ash && (properties.shearArea = Number(t.Ash)), properties.secondAreaMoment = Number(t.I), [e, t.name, properties]) : (properties.modulusE = 2e11, properties.calculateModulusG(), properties.area = .2 * .2, properties.secondAreaMoment = .2 * .2 * .2 * .2 / 12, [0, "BUILTIN_SECTION", properties])
}

function ifaceCheckModelGUI() {
	for (var e = g_Struct.beams.getBeamIDs(), t = 0, s = 0, i = 0; i < e.length; i++) {
		var r = e[i],
			r = g_Struct.beams.getBeamSection(r);
		r && g_Struct.cache.getSection(r) ? t++ : s++
	}
	var a = ifaceGetDefaultSection(),
		o = a[0],
		a = a[1];
	0 < t && 0 < s ? message("Warning: some beams were not assigned a section. Using section " + o + ": " + a) : 0 === t && s === e.length && message("Warning: beams were not assigned a section. Using section " + o + ": " + a), isEntityBehindMargin()
}

function ifaceGetNodeByID(e, t) {
	for (var s = null, i = 0; i < t.length; ++i)
		if (e == t[i][0]) {
			s = t[i][1];
			break
		} return s
}

function ifaceGetBeamByID(e, t) {
	return ifaceGetNodeByID(e, t)
}

function ifaceGetPropertiesOfSection(e) {
	e = g_Struct.cache.getSection(e);
	return properties = new Properties, properties.transversalContractionCoef = .3, properties.modulusE = e.E, properties.calculateModulusG(), properties.area = e.A, e.Ash && (properties.shearArea = e.Ash), properties.secondAreaMoment = e.I, properties
}

function ifaceCreateBeams(e) {
	for (var t = [], s = g_Struct.beams.getBeamIDs(), i = 0; i < s.length; ++i) {
		var r = s[i],
			a = g_Struct.cache.getBeamNodes(r),
			o = (a[0], a[1], g_Struct.beams.hasHinge(r)),
			n = 1 & o ? "H" : "",
			o = 2 & o ? "H" : "",
			a = new Beam(ifaceGetNodeByID(a[0], e), ifaceGetNodeByID(a[1], e));
		n && (a.hinge0 = !0), o && (a.hinge1 = !0);
		o = g_Struct.beams.getBeamSection(r);
		g_Struct.cache.getSection(o) || (o = null), a.properties = o ? ifaceGetPropertiesOfSection(o) : ifaceGetDefaultSection()[2], t.push([r, a])
	}
	return t
}

function ifaceCreateNodes() {
	for (var e = [], t = g_Struct.nodes.getNodeIDs(), s = 0; s < t.length; ++s) {
		var i, r, a = t[s];
		g_Struct.findBeamsByNode(a).length && (r = g_Struct.cache.getCoordinates(a), i = g_Struct.nodes.getSupport(a), r = new Node0(.001 * r[0], .001 * r[1]), i && (r.restraints = [fnHasX(i), fnHasY(i), fnHasR(i)]), e.push([a, r]))
	}
	return e
}

function ifaceLoadsOnNodes(e, t, s) {
	for (var i = g_Struct.nodes.getNodeIDs(), r = 0; r < i.length; ++r) {
		var a, o, n, c, l = i[r],
			h = g_Struct.nodes.getSingleLoadOnNode(l, s),
			m = ifaceGetNodeByID(l, t);
		h.length && (a = h[0], o = h[1], c = n = 0, f = h[2], a && (o = o * Math.PI / 180, n = a * Math.cos(o), c = a * Math.sin(o)), f = new NodeForceLoad(m, [1e3 * c, 1e3 * -n, 1e3 * f]), e.addLoad(f));
		var f, l = g_Struct.nodes.getDisplacements(l, s),
			l = [.001 * -l[0], .001 * -l[1], 2 * -l[2] * Math.PI / 360];
		(l[0] || l[1] || l[2]) && (f = new NodeDeformationLoad(m, l), e.addLoad(f))
	}
}

function ifaceLoadsOnBeams(e, t, s) {
	for (var i = g_Struct.beams.getBeamIDs(), r = 0; r < i.length; ++r) {
		var a = i[r],
			o = ifaceGetBeamByID(a, t),
			n = g_Struct.beams.getLoadsOnBeam(a, s),
			c = g_Struct.cache.getBeamLength(a);
		if (n.length)
			for (var l = g_Struct.cache.getBeamNodes(a), h = g_Struct.cache.getCoordinates(l[0]), m = g_Struct.cache.getCoordinates(l[1]), a = h[0], l = m[0], h = h[1], m = m[1], f = convertToDirection(a, l, -h, -m), d = 0; d < n.length; ++d) {
				var u, g, p, b, M, v, T, A = n[d];
				"m" == A[0] && (u = A[1], A = new BeamMomentLoad(o, 1e3 * A[2], u / c), e.addLoad(A)), "f" == A[0] && (u = A[1], A = new BeamForceLoad(o, 1e3 * A[2], u / c, b = f - (A[3] ? A[3] / 180 * Math.PI : 0)), e.addLoad(A)), "q" == A[0] && (g = (u = A[1])[0], p = u[1], T = A[3], b = f - A[2] / 180 * Math.PI, M = T[0], v = T[1], T = 0, isNaN(v) || (T = v - M), A = new BeamContinuousLoad(o, 1e3 * M, 1e3 * T, g / c, (p - g) / c, b), e.addLoad(A))
			}
	}
}
var g_iface_error_flag = !1,
	g_LangStrings = {
		MODEL_NOT_RESTRAINED_X: "Model is not restrained in X direction",
		MODEL_NOT_RESTRAINED_Z: "Model is not restrained in Z direction",
		MODEL_NOT_ENOUGH_RESTRAINTS: "Model has not enough restraints",
		MODEL_PROBABLY_NOT_ENOUGH_RESTRAINTS: "Model probably has not enough restraints (according to the number of hinges)",
		MODEL_UNSOLVABLE: "The model can't be solved due to an unsolvable equilibrium system, possibly from a missing restraint or structural instability. This is often caused by hidden flying or unsupported beams in the model.",
		EXTREME_TRANSLATION: "The extreme of a node translation (%%) exceeds a limit. The structure is unstable, or the stiffness is too low for the given load, leading to extreme deformations.",
		EXTREME_ROTATION: "The extreme of a node rotation (%%) exceeds a limit. The structure is unstable, or the stiffness is too low for the given load, leading to extreme deformations.",
		BEAM_ILLEGAL_HEIGHT: "Beam number %% has illegal height of cross-section",
		BEAM_ZERO_HEIGHT: "Beam number %% has zero height cross-section",
		BEAM_ILLEGAL_AREA: "Beam number %% has illegal area of cross-section",
		BEAM_ZERO_AREA: "Beam number %% has zero area cross-section",
		BEAM_ILLEGAL_SHEAR_AREA: "Beam number %% has illegal shear area of cross-section",
		BEAM_ZERO_SHEAR_AREA: "Beam number %% has zero shear area cross-section",
		BEAM_ILLEGAL_SECOND_MOMENT: "Beam number %% has illegal second area moment of cross-section",
		BEAM_ZERO_SECOND_MOMENT: "Beam number %% has zero second area moment of cross-section",
		BEAM_ILLEGAL_ELASTICITY: "Beam number %% has illegal modulus of elasticity",
		BEAM_ZERO_ELASTICITY: "Beam number %% has zero modulus of elasticity",
		BEAM_ILLEGAL_SHEAR_ELASTICITY: "Beam number %% has illegal shear modulus of elasticity",
		BEAM_ZERO_SHEAR_ELASTICITY: "Beam number %% has zero shear modulus of elasticity",
		BEAM_ILLEGAL_TRANS_COEF: "Beam number %% has illegal coefficient of transversal contraction",
		BEAM_ILLEGAL_THERM_COEF: "Beam number %% has illegal coefficient of thermal expansion",
		SOME_BEAMS_NOT_ASSIGNED: "Some beams aren't assigned a cross-section, section '%%' used",
		SECTION_AUTO_ASSIGNED: "Cross-section '%%' assigned to all beams",
		SUCCESSFULLY_SOLVED: "Successfully solved"
	};

function iface_get_log_string(e) {
	var t = g_LangStrings[e];
	return void 0 === t && (t = e), t
}

function iface_beam_id_to_id(e) {
	return console.log(e), g_Struct.solverModel.ifaceBeams[e - 1][0]
}

function iface_log_callback(e, t, s) {
	"warning" == e ? e = "Warning: " : "error" == e && (g_iface_error_flag = !0, e = "ERR: "), 0 == t.indexOf("BEAM_") && (s = iface_beam_id_to_id(s));
	t = iface_get_log_string(t);
	t && void 0 !== s && (t = t.replace("%%", s)), message(e + t)
}

function ifaceNodeReactions(e, t, s) {
	for (var i = e.assembleReactionVector(), r = 0, a = 0, o = t.length, n = 0; n < o; ++n) {
		var c = t[n][0],
			l = t[n][1],
			h = e.assembleNodeReactionVector(l, i);
		a += Math.abs(h[0]) + Math.abs(h[1]), r += Math.abs(h[2])
	}
	for (var m = r / o / 1e7, f = a / o / 2e7, n = 0; n < o; ++n) {
		var c = t[n][0],
			l = t[n][1],
			h = e.assembleNodeReactionVector(l, i),
			d = 0,
			u = 0,
			g = 0,
			p = g_Struct.nodes.getSupport(c);
		Math.abs(h[0]) > f && fnHasX(p) && (d = h[0]), Math.abs(h[1]) > f && fnHasY(p) && (u = h[1]), Math.abs(h[2]) > m && fnHasR(p) && (g = h[2]), (d || u || g) && g_Struct.drawReaction(c, d / 1e3, u / 1e3, g / 1e3, s)
	}
}

function ifaceGetResults(e, t, s) {
	for (var i = t.length, r = [], a = 0; a < i; ++a) {
		for (var o = t[a][0], n = t[a][1], c = g_Struct.beams.getNodesOfInterest(o, s), l = c.length, h = [Array(l), Array(l), Array(l)], m = [0, 0, 0], f = 0; f < l; ++f) {
			var d = n.calculateInternalForces(c[f], e);
			if (0 < f)
				for (var u = c[f] - c[f - 1], g = 0; g <= 2; ++g) {
					var p = h[g][m[g] - 1][1],
						b = d[g];
					(p < 0 && 0 < b || 0 < p && b < 0) && (b = p - b, b = Math.abs(p / b * u), h[g][m[g]] = [c[f - 1] + b, 0], m[g] = m[g] + 1)
				}
			for (g = 0; g <= 2; ++g) h[g][m[g]] = [c[f], d[g]], m[g] = m[g] + 1
		}
		r.push([o, h[2], h[0], h[1]])
	}
	return r
}

function ifaceGetDeflectedBeams(e, t, s) {
	for (var i = [], r = t.length, a = 0; a < r; ++a) {
		for (var o = t[a][0], n = t[a][1], c = g_Struct.beams.getNodesOfInterest(o, s), l = c.length, h = Array(l), m = 0; m < l; ++m) h[m] = n.globalDisplacement(c[m], e);
		i.push([o, c, h])
	}
	return i
}

function ifaceGetDeflectedNodes(e, t) {
	for (var s = t.length, i = Array(s), r = 0; r < s; ++r) {
		var a = t[r][0],
			o = t[r][1],
			o = e.assembleTotalNodeDisplacementVector(o);
		i[r] = [a, o]
	}
	return i
}

function iface_redrawAllResults(e, t) {
	var s, i;
	t = Number(t), e && e.model && e.model[t] && (ifaceNodeReactions(e.model[t], e.ifaceNodes[t], t), s = ifaceGetResults(e.model[t], e.ifaceBeams[t], t), i = ifaceGetDeflectedNodes(e.model[t], e.ifaceNodes[t]), e = ifaceGetDeflectedBeams(e.model[t], e.ifaceBeams[t], t), g_Struct.drawResults(s, i, e, t))
}

function iface_redrawd(e, t) {
	var s;
	t = Number(t), e && e.model && e.model[t] && (s = ifaceGetDeflectedNodes(e.model[t], e.ifaceNodes[t]), e = ifaceGetDeflectedBeams(e.model[t], e.ifaceBeams[t], t), g_Struct.drawDeflectedShape(s, e, t))
}

function iface_redraw(e, t, s) {
	s = Number(s), t && t.model && t.model[s] && (t = ifaceGetResults(t.model[s], t.ifaceBeams[s], s), e.call(g_Struct, t, s))
}

function iface_redrawM(e, t) {
	iface_redraw(g_Struct.drawResultsM, e, t)
}

function iface_redrawV(e, t) {
	iface_redraw(g_Struct.drawResultsV, e, t)
}

function iface_redrawN(e, t) {
	iface_redraw(g_Struct.drawResultsN, e, t)
}
solver_registerMessagesCallback(iface_log_callback);
var g_newDebugWindow = null,
	g_threadSolver = null;

function _calculate(e, t, s, i) {
	if (void 0 === e.model[t])
		if (message("Solving..."), e.ifaceNodes[t] = ifaceCreateNodes(), e.ifaceBeams[t] = ifaceCreateBeams(e.ifaceNodes[t]), e.ifaceBeams[t].length) {
			e.model[t] = new Model;
			for (var r = 0; r < e.ifaceNodes[t].length; ++r) {
				var a = e.ifaceNodes[t][r];
				e.model[t].addNode(a[1])
			}
			for (r = 0; r < e.ifaceBeams[t].length; ++r) {
				var o = e.ifaceBeams[t][r];
				e.model[t].addBeam(o[1])
			}
			ifaceLoadsOnNodes(e.model[t], e.ifaceNodes[t], t), ifaceLoadsOnBeams(e.model[t], e.ifaceBeams[t], t);
			var n = Date.now();
			ifaceCheckModelGUI();
			var c = e.model[t];
			preCheck(c), c.solve(), s && (s = generateXML(c), c = generateHTML(c), !g_newDebugWindow || g_newDebugWindow.closed ? g_newDebugWindow = window.open("about:blank", "debug" + t, "height=600,width=400") : (g_newDebugWindow.focus(), g_newDebugWindow.document = ""), g_newDebugWindow.document.write(s + c), g_newDebugWindow.document.close()), postCheck(e.model[t]), 0 == g_iface_error_flag ? (i && g_Struct.resultsRedraw(t), i = Date.now(), message("Solved in " + (i - n) + " ms")) : e.reset()
		} else message("Nothing to solve, no beam exists")
}

function calculate(e, t, s) {
	g_iface_error_flag = !1, t = Number(t), g_Struct.cache.fill(), e.model || (e.model = [], e.ifaceNodes = [], e.ifaceBeams = []), null === g_threadSolver && (g_threadSolver = setTimeout(function() {
		_calculate(e, t, s, !0), g_threadSolver = null
	}, 0))
}

function Node0(e, t) {
	this.x = e, this.z = t, this.restraints = new Array(!1, !1, !1), this.distanceTo = function(e) {
		var t = e.x - this.x,
			e = e.z - this.z;
		return Math.sqrt(t * t + e * e)
	}, this.toString = function() {
		var e = "Node(";
		return e += "x=" + this.x.toString() + ",", e += "z=" + this.z.toString() + ",", e += "restraintX=" + this.restraints[0].toString() + ",", e += "restraintZ=" + this.restraints[1].toString() + ",", e += "restraintR=" + this.restraints[2].toString() + ")"
	}
}

function Properties() {
	this.modulusE = 0, this.modulusG = 1e99, this.transversalContractionCoef = .3, this.thermalExpansionCoef = .001, this.height = 0, this.area = 0, this.shearArea = 1e99, this.secondAreaMoment = 0, this.calculateModulusG = function() {
		this.modulusG = this.modulusE / 2 / (1 + this.transversalContractionCoef)
	}, this.toString = function() {
		var e = "Properties(";
		return e += "modulusE=" + this.modulusE.toString() + ",", e += "modulusG=" + this.modulusG.toString() + ",", e += "transversalContractionCoef=" + this.transversalContractionCoef.toString() + ",", e += "thermalExpansionCoef=" + this.thermalExpansionCoef.toString() + ",", e += "height=" + this.height.toString() + ",", e += "area=" + this.area.toString() + ",", e += "shearArea=" + this.shearArea.toString() + ",", e += "secondAreaMoment=" + this.secondAreaMoment.toString() + ")"
	}
}

function Beam(e, t) {
	this.node0 = e, this.node1 = t, this.hinge0 = !1, this.hinge1 = !1, this.properties = new Properties, this.stiffnessMatrix = null, this.globalStiffnessMatrix = null, this.length = null, this.cosinus = null, this.sinus = null, this.angle = null, this.shearConstant = null, this.localPrimaryForceVector = null, this.localSecondaryForceVector = null, this.localTotalForceVector = null, this.localPrimaryDisplacementVector = null, this.localSecondaryDisplacementVector = null, this.secondaryDisplacementFiaShear = null, this.secondaryDisplacementFibShear = null, this.primaryDisplacementFiaShear = null, this.primaryDisplacementFibShear = null, this.calculateLength = function() {
		this.length = e.distanceTo(t)
	}, this.calculateShearConstant = function() {
		this.shearConstant = 12 * this.properties.modulusE * this.properties.secondAreaMoment / this.properties.modulusG / this.properties.shearArea / this.length / this.length
	}, this.calculateAngle = function() {
		var e = this.node1.x - this.node0.x,
			t = this.node1.z - this.node0.z;
		this.angle = 0 <= e ? Math.asin(this.sinus) : 0 <= t ? Math.PI - Math.asin(this.sinus) : -Math.PI - Math.asin(this.sinus)
	}, this.calculateGoniometricConstants = function() {
		var e = this.node1.x - this.node0.x,
			t = this.node1.z - this.node0.z;
		this.cosinus = e / this.length, this.sinus = t / this.length
	}, this.update = function() {
		this.calculateLength(), this.calculateGoniometricConstants(), this.calculateAngle(), this.calculateShearConstant()
	}, this.toString = function() {
		var e = "Beam(";
		return e += "node0=" + this.node0.toString() + ",", e += "node1=" + this.node1.toString() + ",", e += "hinge0=" + this.hinge0.toString() + ",", e += "hinge1=" + this.hinge1.toString() + ",", e += "length=" + this.length.toString() + ",", e += "angle=" + this.angle.toString() + ",", e += "properties=" + this.properties.toString() + ",", e += "shearConstant=" + this.shearConstant.toString() + ")"
	}, this.assembleStiffnessMatrix = function() {
		this.update(), this.stiffnessMatrix = matrixAllocate(6, 6);
		var e, t = this.properties.modulusE * this.properties.area / this.length;
		this.stiffnessMatrix[0][0] = t, this.stiffnessMatrix[0][3] = -t, this.stiffnessMatrix[3][0] = -t, this.stiffnessMatrix[3][3] = t, this.hinge0 || this.hinge1 ? !this.hinge0 && this.hinge1 ? (e = 12 * this.properties.modulusE * this.properties.secondAreaMoment / this.length / this.length / (4 + this.shearConstant), this.stiffnessMatrix[1][1] = e / this.length, this.stiffnessMatrix[1][2] = -e, this.stiffnessMatrix[1][4] = -this.stiffnessMatrix[1][1], this.stiffnessMatrix[2][1] = -e, this.stiffnessMatrix[2][2] = e * this.length, this.stiffnessMatrix[2][4] = e, this.stiffnessMatrix[4][1] = this.stiffnessMatrix[1][4], this.stiffnessMatrix[4][2] = this.stiffnessMatrix[2][4], this.stiffnessMatrix[4][4] = this.stiffnessMatrix[1][1]) : this.hinge0 && !this.hinge1 && (e = 12 * this.properties.modulusE * this.properties.secondAreaMoment / this.length / this.length / (4 + this.shearConstant), this.stiffnessMatrix[1][1] = e / this.length, this.stiffnessMatrix[1][4] = -this.stiffnessMatrix[1][1], this.stiffnessMatrix[1][5] = -e, this.stiffnessMatrix[4][1] = this.stiffnessMatrix[1][4], this.stiffnessMatrix[4][4] = this.stiffnessMatrix[1][1], this.stiffnessMatrix[4][5] = e, this.stiffnessMatrix[5][1] = -e, this.stiffnessMatrix[5][4] = this.stiffnessMatrix[4][5], this.stiffnessMatrix[5][5] = e * this.length) : (e = this.properties.modulusE * this.properties.secondAreaMoment / this.length / this.length / (1 + this.shearConstant), this.stiffnessMatrix[1][1] = 12 * e / this.length, this.stiffnessMatrix[1][2] = -6 * e, this.stiffnessMatrix[1][4] = -this.stiffnessMatrix[1][1], this.stiffnessMatrix[1][5] = this.stiffnessMatrix[1][2], this.stiffnessMatrix[2][1] = this.stiffnessMatrix[1][2], this.stiffnessMatrix[2][2] = (4 + this.shearConstant) * e * this.length, this.stiffnessMatrix[2][4] = -this.stiffnessMatrix[1][2], this.stiffnessMatrix[2][5] = (2 - this.shearConstant) * e * this.length, this.stiffnessMatrix[4][1] = this.stiffnessMatrix[1][4], this.stiffnessMatrix[4][2] = this.stiffnessMatrix[2][4], this.stiffnessMatrix[4][4] = this.stiffnessMatrix[1][1], this.stiffnessMatrix[4][5] = this.stiffnessMatrix[2][4], this.stiffnessMatrix[5][1] = this.stiffnessMatrix[1][2], this.stiffnessMatrix[5][2] = this.stiffnessMatrix[2][5], this.stiffnessMatrix[5][4] = this.stiffnessMatrix[2][4], this.stiffnessMatrix[5][5] = this.stiffnessMatrix[2][2])
	}, this.assembleTransformMatrix = function() {
		this.transformMatrix = matrixAllocate(6, 6), this.transformMatrix[0][0] = this.cosinus, this.transformMatrix[1][1] = this.cosinus, this.transformMatrix[3][3] = this.cosinus, this.transformMatrix[4][4] = this.cosinus, this.transformMatrix[2][2] = 1, this.transformMatrix[5][5] = 1, this.transformMatrix[0][1] = this.sinus, this.transformMatrix[3][4] = this.sinus, this.transformMatrix[1][0] = -this.sinus, this.transformMatrix[4][3] = -this.sinus
	}, this.calculateGlobalStiffnessMatrix = function() {
		this.assembleStiffnessMatrix(), this.assembleTransformMatrix();
		var e = matrixMultiply(matrixTranspose(this.transformMatrix), this.stiffnessMatrix);
		this.globalStiffnessMatrix = matrixMultiply(e, this.transformMatrix)
	}, this.calculateGlobalStiffnessMatrix = function() {
		this.assembleStiffnessMatrix(), this.assembleTransformMatrix();
		var e = matrixMultiply(matrixTranspose(this.transformMatrix), this.stiffnessMatrix);
		this.globalStiffnessMatrix = matrixMultiply(e, this.transformMatrix)
	}, this.transformVectorToLocal = function(e) {
		return toVector(matrixMultiply(this.transformMatrix, toMatrix(e)))
	}, this.transformVectorToGlobal = function(e) {
		return toVector(matrixMultiply(matrixTranspose(this.transformMatrix), toMatrix(e)))
	}, this.applyPrimaryDisplacement = function(e) {
		this.localPrimaryDisplacementVector = toVector(matrixMultiply(this.transformMatrix, toMatrix(e)))
	}, this.calculateSecondaryForceVector = function(e) {
		if (!(e instanceof Model)) throw new IllegalArgumentException;
		this.localSecondaryForceVector = toVector(matrixMultiply(this.stiffnessMatrix, toMatrix(this.localSecondaryDisplacementVector)))
	}, this.calculatePrimaryForceVectorFromPrimaryDisplacements = function() {
		return toVector(matrixMultiply(this.stiffnessMatrix, toMatrix(this.localPrimaryDisplacementVector)))
	}, this.calculatePrimaryForceVector = function(e) {
		if (!(e instanceof Model)) throw new IllegalArgumentException;
		this.localPrimaryForceVector = this.calculatePrimaryForceVectorFromPrimaryDisplacements();
		for (var t = 0; t < e.loads.length; t++) {
			var s = e.loads[t];
			s.beam == this && (s.assemblePrimaryForceVector(), vectorAdd(this.localPrimaryForceVector, s.localPrimaryForceVector))
		}
	}, this.calculateResultingVectors = function(e) {
		var t = e.assembleSecondaryBeamGlobalDisplacementVector(this);
		this.localSecondaryDisplacementVector = this.transformVectorToLocal(t), this.calculateResultingForceVectors(e), this.secondaryDisplacementFiaShear = -this.localSecondaryForceVector[1] / this.properties.modulusG / this.properties.shearArea, this.secondaryDisplacementFibShear = this.localSecondaryForceVector[4] / this.properties.modulusG / this.properties.shearArea, this.primaryDisplacementFiaShear = 0, this.primaryDisplacementFibShear = 0
	}, this.calculateResultingForceVectors = function(e) {
		this.calculateSecondaryForceVector(e), this.localTotalForceVector = vectorAllocate(6), vectorAdd(this.localTotalForceVector, this.localPrimaryForceVector), vectorAdd(this.localTotalForceVector, this.localSecondaryForceVector)
	}, this.calculateInternalForces = function(e, t) {
		var s = vectorAllocate(3);
		return s[0] = -this.localTotalForceVector[0], s[1] = -this.localTotalForceVector[1], s[2] = -this.localTotalForceVector[2] + s[1] * e * this.length, this.addInternalForcesContributions(e, s, t), s
	}, this.addInternalForcesContributions = function(e, t, s) {
		for (var i = 0; i < s.loads.length; i++) {
			var r = s.loads[i];
			r.beam == this && vectorAdd(t, r.calculateContributionToInternalForces(e))
		}
	}, this.globalDisplacement = function(e, t) {
		var s = this.localDisplacement(e, t),
			e = s[0],
			t = s[1],
			s = -t * this.sinus + e * this.cosinus,
			e = t * this.cosinus + e * this.sinus;
		return new Array(s, e)
	}, this.localDisplacement = function(e, t) {
		for (var s = e * this.length, i = new Array(this.calculateSecondaryDisplacementU(s), this.calculateSecondaryDisplacementW(s), this.calculateSecondaryDisplacementFi(s)), r = 0; r < t.loads.length; r++) {
			var a = t.loads[r];
			a.beam == this && vectorAdd(i, a.calculateContributionToDisplacements(e))
		}
		return i
	}, this.calculateSecondaryDisplacementU = function(e) {
		var t = this.localPrimaryDisplacementVector[0] + this.localSecondaryDisplacementVector[0];
		return t + e * ((this.localPrimaryDisplacementVector[3] + this.localSecondaryDisplacementVector[3] - t) / this.length)
	}, this.calculateSecondaryDisplacementW = function(e) {
		var t, s = this.length,
			i = this.localPrimaryDisplacementVector[1] + this.localSecondaryDisplacementVector[1],
			r = this.localPrimaryDisplacementVector[2] + this.localSecondaryDisplacementVector[2],
			a = this.localPrimaryDisplacementVector[4] + this.localSecondaryDisplacementVector[4],
			o = this.localPrimaryDisplacementVector[5] + this.localSecondaryDisplacementVector[5],
			n = r - (this.primaryDisplacementFiaShear + this.secondaryDisplacementFiaShear),
			r = o - (this.primaryDisplacementFibShear + this.secondaryDisplacementFibShear);
		return this.hinge0 || this.hinge1 ? !this.hinge0 && this.hinge1 ? i + e * (-n + e * (-3 * (t = -((a - i) / s + n) / s / s / 2) * s + e * t)) : this.hinge0 && !this.hinge1 ? a + (o = s - e) * (r + o * (-3 * (t = -((i - a) / s - r) / s / s / 2) * s + o * t)) : i + e * ((a - i) / s) : i + e * (-n + e * (((3 * a - 3 * i) / s + 2 * n + r) / s + e * (t = ((2 * i - 2 * a) / s - n - r) / s / s)))
	}, this.calculateSecondaryDisplacementFi = function(e) {
		return 0
	}
}

function GaussElimination() {
	this.pivots = null, this.determinant = null, this.solve = function(e) {
		return this.forward(e), this.backward(e)
	}, this.forward = function(e) {
		var t = e.length,
			s = e[0].length;
		if (this.pivots = new Array, this.pivots[t - 1] = 1, 0 != t) {
			for (var i = 0; i < t - 1; i++) {
				for (var r = Math.abs(e[i][i]), a = i, o = i + 1; o < t; o++) {
					var n = Math.abs(e[o][i]);
					r < n && (r = n, a = o)
				}
				r = e[a][i], this.pivots[i] = r;
				for (var c = i; c < s; c++) {
					var l = e[a][c] / r;
					e[a][c] = e[i][c], e[i][c] = l
				}
				for (o = i + 1; o < t; o++)
					for (c = i + 1; c < s; c++) e[o][c] -= e[i][c] * e[o][i]
			}
			this.calculateDeterminant(e)
		}
	}, this.backward = function(e) {
		var t = e.length,
			s = e[0].length,
			i = new Array,
			r = e[t - 1][s - 1] / e[t - 1][t - 1];
		i[t - 1] = r;
		for (var a = t - 2; 0 <= a; a--) {
			for (var o = e[a][s - 1], n = a + 1; n < t; n++) o -= e[a][n] * i[n];
			i[a] = o
		}
		return i
	}, this.calculateDeterminant = function(e) {
		var t = e.length;
		this.determinant = -1;
		for (var s = 0; s < t; s++) this.determinant *= e[s][s] * this.pivots[s]
	}
}

function MatrixException() {}

function IllegalArgumentException() {}

function matrixAllocate(e, t) {
	for (var s = new Array, i = 0; i < e; i++) {
		s[i] = new Array;
		for (var r = 0; r < t; r++) s[i][r] = 0
	}
	return s
}

function vectorAllocate(e) {
	for (var t = new Array, s = 0; s < e; s++) t[s] = 0;
	return t
}

function toMatrix(e) {
	if (!(e instanceof Array)) throw new IllegalArgumentException;
	for (var t = e.length, s = matrixAllocate(t, 1), i = 0; i < t; i++) s[i][0] = e[i];
	return s
}

function toVector(e) {
	var t = e.length;
	if (1 != e[0].length) throw new IllegalArgumentException;
	for (var s = vectorAllocate(t), i = 0; i < t; i++) s[i] = e[i][0];
	return s
}

function matrixAugment(e, t) {
	var s = e.length,
		i = e[0].length,
		r = t.length,
		a = t[0].length,
		o = s,
		n = i + a;
	if (s != r) throw new IllegalArgumentException;
	for (var c = matrixAllocate(o, n), l = 0; l < s; l++)
		for (var h = 0; h < i; h++) c[l][h] = e[l][h];
	for (l = 0; l < r; l++)
		for (h = 0; h < a; h++) c[l][h + i] = t[l][h];
	return c
}

function matrixMultiply(e, t) {
	var s = e.length,
		i = e[0].length,
		r = t.length,
		a = s,
		o = t[0].length;
	if (i != r) throw new IllegalArgumentException;
	for (var n = matrixAllocate(a, o), c = 0; c < a; c++)
		for (var l = 0; l < o; l++) {
			for (var h = 0, m = 0; m < s; m++) h += e[c][m] * t[m][l];
			n[c][l] = h
		}
	return n
}

function vectorAdd(e, t) {
	var s = e.length;
	if (s != t.length) throw new IllegalArgumentException;
	for (var i = 0; i < s; i++) e[i] += t[i]
}

function matrixSolve(e, t) {
	t = matrixAugment(e, toMatrix(t));
	return (new GaussElimination).solve(t)
}

function matrixTranspose(e) {
	for (var t = e.length, s = e[0].length, i = matrixAllocate(s, t), r = 0; r < t; r++)
		for (var a = 0; a < s; a++) i[a][r] = e[r][a];
	return i
}

function vectorToTable(e) {
	return matrixToTable(toMatrix(e))
}

function matrixToTable(e) {
	for (var t = "<TABLE border='1'>", s = 0; s < e.length; s++) {
		t += "<TR>";
		for (var i = 0; i < e[0].length; i++) t += "<TD>", t += e[s][i].toString(), t += "</TD>";
		t += "</TR>"
	}
	return t += "</TABLE>"
}

function generalDisplacementWMa(e, t) {
	return -2 * e / t * (e - t) * (e - 2 * t)
}

function generalDisplacementWMb(e, t) {
	return 2 * e / t * (t * t - e * e)
}

function generalDisplacementFiMa(e, t) {
	return -2 / t * (3 * e * e - 6 * t * e + 2 * t * t)
}

function generalDisplacementFiMb(e, t) {
	return 2 / t * (t * t - 3 * e * e)
}

function NodeForceLoad(e, t) {
	this.node = e, this.components = t, this.toString = function() {
		var e = "NodeForceLoad(";
		return e += "node=" + this.node.toString() + ",", e += "componentX=" + this.components[0].toString() + ",", e += "componentZ=" + this.components[1].toString() + ",", e += "componentR=" + this.components[2].toString() + ","
	}
}

function Model() {
	this.nodes = new Array, this.beams = new Array, this.loads = new Array, this.stiffnessMatrix = null, this.nodeLoadVector = null, this.primaryLoadVector = null, this.loadVector = null, this.primaryDisplacementVector = null, this.secondaryDisplacementVector = null, this.secondaryLoadVector = null, this.reactionVector = null, this.dof = function() {
		return 3 * this.nodes.length
	}, this.addNode = function(e) {
		if (!(e instanceof Node0)) throw new IllegalArgumentException;
		this.nodes[this.nodes.length] = e
	}, this.addBeam = function(e) {
		if (!(e instanceof Beam)) throw new IllegalArgumentException;
		this.beams[this.beams.length] = e
	}, this.addLoad = function(e) {
		if (e instanceof NodeDeformationLoad) this.loads[this.loads.length] = e;
		else if (e instanceof NodeForceLoad) this.loads[this.loads.length] = e;
		else if (e instanceof BeamForceLoad) this.loads[this.loads.length] = e;
		else if (e instanceof BeamMomentLoad) this.loads[this.loads.length] = e;
		else {
			if (!(e instanceof BeamContinuousLoad)) throw new IllegalArgumentException;
			this.loads[this.loads.length] = e
		}
	}, this.toString = function() {
		var e = "Model(";
		return e += "nodeCount=" + this.nodes.length.toString() + ",", e += "beamCount=" + this.beams.length.toString() + ",", e += "loadCount=" + this.loads.length.toString() + ",", e += "dof=" + this.dof().toString() + ")"
	}, this.solve = function() {
		this.assembleStiffnessMatrix(), this.assembleLoadVectors(), this.applyRestraints(), this.secondaryDisplacementVector = matrixSolve(this.stiffnessMatrix, this.loadVector), this.makePostprocess()
	}, this.makePostprocess = function() {
		for (var e = 0; e < this.beams.length; e++) this.beams[e].calculateResultingVectors(this);
		this.assembleReactionVector()
	}, this.assembleLoadVectors = function() {
		this.assemblePrimaryDisplacementVector(), this.applyPrimaryDisplacementsToBeams(), this.assembleNodeLoadVector(), this.assemblePrimaryLoadVector(), this.assembleLoadVector()
	}, this.assemblePrimaryDisplacementVector = function() {
		var e = this.dof();
		this.primaryDisplacementVector = vectorAllocate(e);
		for (var t = 0; t < this.loads.length; t++) this.applyNodeDeformationLoad(this.loads[t])
	}, this.applyPrimaryDisplacementsToBeams = function() {
		for (var e = 0; e < this.beams.length; e++) {
			for (var t = this.beams[e], s = t.node0, i = t.node1, r = this.nodes.indexOf(s), a = this.nodes.indexOf(i), o = vectorAllocate(6), n = 0; n < 3; n++) o[n] = this.primaryDisplacementVector[3 * r + n];
			for (n = 0; n < 3; n++) o[3 + n] = this.primaryDisplacementVector[3 * a + n];
			t.applyPrimaryDisplacement(o)
		}
	}, this.applyNodeDeformationLoad = function(e) {
		if (e instanceof NodeDeformationLoad)
			for (var t = e.node, s = e.components, i = 3 * this.nodes.indexOf(t), r = 0; r < 3; r++) this.primaryDisplacementVector[i + r] += s[r]
	}, this.assembleNodeLoadVector = function() {
		var e = this.dof();
		this.nodeLoadVector = vectorAllocate(e);
		for (var t = 0; t < this.loads.length; t++) this.applyNodeForceLoad(this.loads[t])
	}, this.applyNodeForceLoad = function(e) {
		if (e instanceof NodeForceLoad)
			for (var t = e.node, s = e.components, i = 3 * this.nodes.indexOf(t), r = 0; r < 3; r++) this.nodeLoadVector[i + r] += s[r]
	}, this.calculatePrimaryForcesOfBeams = function() {
		for (var e = 0; e < this.beams.length; e++) this.beams[e].calculatePrimaryForceVector(this)
	}, this.assemblePrimaryLoadVector = function() {
		this.calculatePrimaryForcesOfBeams();
		var e = this.dof();
		this.primaryLoadVector = vectorAllocate(e);
		for (var t = 0; t < this.beams.length; t++) this.addToPrimaryLoadVector(this.beams[t])
	}, this.addToPrimaryLoadVector = function(e) {
		for (var t = this.nodes.indexOf(e.node0), s = this.nodes.indexOf(e.node1), i = e.transformVectorToGlobal(e.localPrimaryForceVector), r = 0; r < 3; r++) this.primaryLoadVector[3 * t + r] += i[r];
		for (r = 0; r < 3; r++) this.primaryLoadVector[3 * s + r] += i[r + 3]
	}, this.assembleLoadVector = function() {
		var e = this.dof();
		this.loadVector = vectorAllocate(e);
		for (var t = 0; t < e; t++) this.loadVector[t] = this.nodeLoadVector[t] - this.primaryLoadVector[t]
	}, this.assemblePrimaryNodeDisplacementVector = function(e) {
		for (var t = this.nodes.indexOf(e), s = vectorAllocate(3), i = 0; i < 3; i++) s[i] = this.primaryDisplacementVector[3 * t + i];
		return s
	}, this.assembleSecondaryNodeDisplacementVector = function(e) {
		for (var t = this.nodes.indexOf(e), s = vectorAllocate(3), i = 0; i < 3; i++) s[i] = this.secondaryDisplacementVector[3 * t + i];
		return s
	}, this.assembleTotalNodeDisplacementVector = function(e) {
		var t = this.assemblePrimaryNodeDisplacementVector(e),
			s = this.assembleSecondaryNodeDisplacementVector(e),
			e = vectorAllocate(t.length);
		return vectorAdd(e, t), vectorAdd(e, s), e
	}, this.assembleSecondaryBeamGlobalDisplacementVector = function(e) {
		for (var t = this.assembleSecondaryNodeDisplacementVector(e.node0), s = this.assembleSecondaryNodeDisplacementVector(e.node1), i = vectorAllocate(6), r = 0; r < 3; r++) i[r] = t[r];
		for (r = 0; r < 3; r++) i[r + 3] = s[r];
		return i
	}, this.assembleStiffnessMatrix = function() {
		var e = this.dof();
		this.stiffnessMatrix = matrixAllocate(e, e);
		for (var t = 0; t < this.beams.length; t++) this.addStiffnessOfBeam(this.beams[t])
	}, this.addStiffnessOfBeam = function(e) {
		var t = this.nodes.indexOf(e.node0),
			s = this.nodes.indexOf(e.node1);
		e.calculateGlobalStiffnessMatrix();
		for (var i = e.globalStiffnessMatrix, r = new Array(3 * t, 3 * t + 1, 3 * t + 2, 3 * s, 3 * s + 1, 3 * s + 2), a = 0; a < 6; a++)
			for (var o = 0; o < 6; o++) this.stiffnessMatrix[r[a]][r[o]] += i[a][o]
	}, this.applyRestraints = function() {
		for (var e = 0; e < this.nodes.length; e++) this.applyRestraintsOfNode(this.nodes[e])
	}, this.applyRestraintsOfNode = function(e) {
		for (var t = this.dof(), s = e.restraints, i = this.nodes.indexOf(e), r = this.isNodeRotationFree(e), a = 0; a < 3; a++)
			if (s[a] || 2 == a && r) {
				for (var o = 3 * i + a, n = 0; n < t; n++) o == n ? this.stiffnessMatrix[n][n] = 1 : (this.stiffnessMatrix[o][n] = 0, this.stiffnessMatrix[n][o] = 0);
				this.loadVector[o] = 0
			}
	}, this.applyRestraintsOfNodes = function(e) {
		for (var t = this.dof(), s = e.restraints, i = this.nodes.indexOf(e), r = this.isNodeRotationFree(e), a = 0; a < 3; a++)
			if (s[a] || 2 == a && r) {
				for (var o = 3 * i + a, n = 0; n < t; n++) o == n ? this.stiffnessMatrix[n][n] = 1 : (this.stiffnessMatrix[o][n] = 0, this.stiffnessMatrix[n][o] = 0);
				this.loadVector[o] = 0
			}
	}, this.isNodeRotationFree = function(e) {
		for (var t = 0; t < this.beams.length; t++) {
			var s = this.beams[t];
			if (s.node0 == e && !s.hinge0) return !1;
			if (s.node1 == e && !s.hinge1) return !1
		}
		return !0
	}, this.assembleReactionVector = function() {
		var e = this.dof();
		this.reactionVector = vectorAllocate(e);
		for (var t = 0; t < this.beams.length; t++) {
			for (var s = this.beams[t], i = this.nodes.indexOf(s.node0), r = this.nodes.indexOf(s.node1), a = s.transformVectorToGlobal(s.localTotalForceVector), o = 0; o < 3; o++) this.reactionVector[3 * i + o] += a[o];
			for (o = 0; o < 3; o++) this.reactionVector[3 * r + o] += a[o + 3]
		}
		for (t = 0; t < e; t++) this.reactionVector[t] -= this.nodeLoadVector[t]
	}, this.assembleSecondaryLoadVector = function() {
		var e = this.dof();
		this.secondaryLoadVector = vectorAllocate(e);
		for (var t = 0; t < this.beams.length; t++) {
			for (var s = this.beams[t], i = this.nodes.indexOf(s.node0), r = this.nodes.indexOf(s.node1), a = s.transformVectorToGlobal(s.localSecondaryForceVector), o = 0; o < 3; o++) this.secondaryLoadVector[3 * i + o] += a[o];
			for (o = 0; o < 3; o++) this.secondaryLoadVector[3 * r + o] += a[o + 3]
		}
	}, this.assembleNodeReactionVector = function(e) {
		for (var e = this.nodes.indexOf(e), t = vectorAllocate(3), s = 3 * e, i = 0; i < 3; i++) t[i] = this.reactionVector[s + i];
		return t
	}
}

function createDefaultBeam(e, t, s) {
	e = new Beam(e, t), t = null;
	return s ? (t = s).shearArea = t.area : ((t = new Properties).modulusE = 1e10, t.transversalContractionCoef = .3, t.calculateModulusG(), t.area = .01, t.shearArea = 1e99, t.secondAreaMoment = .001), e.properties = t, e
}

function createTestBeam(e, t) {
	e = new Beam(e, t), t = null;
	return (t = new Properties).modulusE = 1e10, t.transversalContractionCoef = .3, t.calculateModulusG(), t.area = .01, t.shearArea = 1e99, t.secondAreaMoment = .001, e.properties = t, e
}

function writeTestBeam() {
	var e = createTestBeam(new Node0(0, 0), new Node0(3, 4));
	document.write(e.toString() + "<BR>"), e.calculateGlobalStiffnessMatrix(), document.write(matrixToTable(e.stiffnessMatrix)), document.write(matrixToTable(e.transformMatrix)), document.write(matrixToTable(e.globalStiffnessMatrix))
}

function createFrame() {
	var e = new Node0(0, 0),
		t = new Node0(3, 4),
		s = new Node0(5, 4),
		i = createTestBeam(e, t),
		r = createTestBeam(t, s),
		a = new NodeForceLoad(s, new Array(0, 12, 0)),
		o = new BeamForceLoad(i, 5, .4, .75 * Math.PI),
		n = new BeamMomentLoad(i, 10, .6),
		c = new BeamContinuousLoad(i, 1, .3, .2, .6, .75 * Math.PI),
		l = new Model;
	return l.addNode(e), l.addNode(t), l.addNode(s), l.addBeam(i), l.addBeam(r), l.addLoad(a), l.addLoad(o), l.addLoad(n), l.addLoad(c), e.restraints = new Array(!0, !0, !0), l.solve(), l
}

function createBeam() {
	var e = new Node0(0, 0),
		t = new Node0(6, 0),
		s = createTestBeam(e, t),
		i = (new NodeForceLoad(t, new Array(1, 0, 0)), new NodeDeformationLoad(t, new Array(0, .001, 0))),
		r = (new BeamForceLoad(s, 1, .55, .65 * Math.PI), new BeamContinuousLoad(s, 1, .3, .2, .6, .75 * Math.PI), new BeamMomentLoad(s, 10, .6), new Model);
	return r.addNode(e), r.addNode(t), r.addBeam(s), r.addLoad(i), e.restraints = new Array(!0, !0, !0), t.restraints = new Array(!1, !0, !1), preCheck(r), r.solve(), postCheck(r), r
}

function main() {
	var e = createBeam();
	document.write(e.toString() + "<BR>"), document.write("model.displacementVector=" + vectorToTable(e.displacementVector) + "<BR>"), document.write("model.reactionVector=" + vectorToTable(e.reactionVector) + "<BR>"), document.write("model.nodes[0].nodeDisplacementVector=" + vectorToTable(e.assembleNodeDisplacementVector(e.nodes[0])) + "<BR>");
	document.write("beam0.internalForces(0.45)=" + vectorToTable(e.beams[0].calculateInternalForces(.45, e)) + "<BR>"), document.write("beam0.localDisplacement(0.45)=" + vectorToTable(e.beams[0].localDisplacement(.45, e)) + "<BR>"), document.write("xml=" + generateXML(e) + "<BR>"), document.write("html=" + generateHTML(e) + "<BR>")
}

function generateXML(e) {
	if (!(e instanceof Model)) throw new IllegalArgumentException;
	for (var t = "&lt;STRUCTURE&gt;<BR>", s = 0; s < e.nodes.length; s++) t += generateNodeTag(e.nodes[s]);
	for (s = 0; s < e.beams.length; s++) t += generateMaterialTag(e.beams[s]);
	for (s = 0; s < e.beams.length; s++) t += generateCrosssectionTag(e.beams[s]);
	for (s = 0; s < e.beams.length; s++) t += generateBeamTag(e.beams[s], e);
	for (s = 0; s < e.loads.length; s++) t += generateLoadTag(e.loads[s], e);
	return t + "&lt;/STRUCTURE&gt;<BR>"
}

function generateNodeTag(e) {
	var t = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;NODE";
	return t += " coordX='" + e.x + "'", t += " coordY='" + -e.z + "'", t += " restraintX='" + e.restraints[0] + "'", t += " restraintY='" + e.restraints[1] + "'", t += " restraintRotation='" + e.restraints[2] + "'", t += " /&gt<BR>"
}

function generateMaterialTag(e) {
	var t = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;MATERIAL";
	return t += " elasticityModulus='" + e.properties.modulusE + "'", t += " contractionCoefficient='" + e.properties.transversalContractionCoef + "'", t += " thermalExpansionCoefficient='" + e.properties.thermalExpansionCoef + "'", t += " /&gt<BR>"
}

function generateCrosssectionTag(e) {
	var t = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;CROSSSECTION";
	return t += " height='" + e.properties.height + "'", t += " area='" + e.properties.area + "'", t += " shearArea='" + e.properties.shearArea + "'", t += " secondAreaMoment='" + e.properties.secondAreaMoment + "'", t += " /&gt<BR>"
}

function generateBeamTag(e, t) {
	var s = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;BAR";
	return s += " node1Index='" + t.nodes.indexOf(e.node0) + "'", s += " node2Index='" + t.nodes.indexOf(e.node1) + "'", s += " separator1Rotation='" + e.hinge0 + "'", s += " separator2Rotation='" + e.hinge1 + "'", s += " materialIndex='" + t.beams.indexOf(e) + "'", s += " crosssectionIndex='" + t.beams.indexOf(e) + "'", s += " /&gt<BR>"
}

function generateLoadTag(e, t) {
	if (e instanceof NodeDeformationLoad) return generateNodeDeformationLoadTag(e, t);
	if (e instanceof NodeForceLoad) return generateNodeForceLoadTag(e, t);
	if (e instanceof BeamForceLoad) return generateBeamForceLoadTag(e, t);
	if (e instanceof BeamMomentLoad) return generateBeamMomentLoadTag(e, t);
	if (e instanceof BeamContinuousLoad) return generateBeamContinuousLoadTag(e, t);
	throw new IllegalArgumentException
}

function generateNodeDeformationLoadTag(e, t) {
	var s = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;NODEDEFORMATIONLOAD";
	return s += " nodeIndex='" + t.nodes.indexOf(e.node) + "'", s += " x='" + e.components[0] + "'", s += " y='" + -e.components[1] + "'", s += " rotation='" + e.components[2] + "'", s += " /&gt<BR>"
}

function generateNodeForceLoadTag(e, t) {
	var s = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;NODEFORCELOAD";
	return s += " nodeIndex='" + t.nodes.indexOf(e.node) + "'", s += " x='" + e.components[0] + "'", s += " y='" + -e.components[1] + "'", s += " rotation='" + e.components[2] + "'", s += " /&gt<BR>"
}

function generateBeamContinuousLoadTag(e, t) {
	var s = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;BARCONTINUOUSLOAD",
		i = convertAngle(e.angle);
	return s += " barIndex='" + t.beams.indexOf(e.beam) + "'", s += " size='" + e.size + "'", s += " sizeChange='" + e.sizeChange + "'", s += " position='" + e.position + "'", s += " length='" + e.length + "'", s += " angle='" + i + "'", s += " inputPositionRelative='true'", s += " inputAngleLocal='true'", s += " angleUnit='deg'", s += " /&gt<BR>"
}

function generateBeamForceLoadTag(e, t) {
	var s = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;BARFORCE",
		i = convertAngle(e.angle);
	return s += " barIndex='" + t.beams.indexOf(e.beam) + "'", s += " size='" + e.size + "'", s += " position='" + e.position + "'", s += " angle='" + i + "'", s += " inputPositionRelative='true'", s += " inputAngleLocal='true'", s += " angleUnit='deg'", s += " /&gt<BR>"
}

function generateBeamMomentLoadTag(e, t) {
	var s = "&nbsp;&nbsp;&nbsp;&nbsp;&lt;BARMOMENT";
	return s += " barIndex='" + t.beams.indexOf(e.beam) + "'", s += " size='" + e.size + "'", s += " position='" + e.position + "'", s += " inputPositionRelative='true'", s += " /&gt<BR>"
}

function convertAngle(e) {
	e = 180 * e / Math.PI;
	return 360 <= (e += 180) && (e -= 360), e
}

function BeamForceLoad(e, t, s, i) {
	this.beam = e, this.size = t, this.position = s, this.angle = i, this.localPrimaryForceVector = null, this.toString = function() {
		var e = "BeamForceLoad(";
		return e += "beam=" + this.beam.toString() + ",", e += "size=" + this.size.toString() + ",", e += "position=" + this.position.toString() + ",", e += "angle=" + this.angle.toString() + ","
	}, this.assemblePrimaryForceVector = function() {
		var e = this.beam.length,
			t = this.beam.shearConstant,
			s = Math.cos(Math.PI - this.angle),
			i = Math.sin(Math.PI - this.angle),
			r = e * this.position,
			a = this.size * s,
			o = this.size * i,
			s = r,
			i = e - s,
			r = e * e * e;
		this.localPrimaryForceVector = vectorAllocate(6), this.localPrimaryForceVector[0] = -a * i / e, this.localPrimaryForceVector[3] = -a * s / e, this.beam.hinge0 || this.beam.hinge1 ? !this.beam.hinge0 && this.beam.hinge1 ? (this.localPrimaryForceVector[1] = -o * i / (r * (4 + t)) * (t * e * e + 2 * (3 * e * e - i * i)), this.localPrimaryForceVector[2] = 2 * o * s * i / (e * e * (4 + t)) * (e + i), this.localPrimaryForceVector[4] = -o * s / (r * (4 + t)) * (t * e * e + 2 * s * (2 * e + i))) : this.beam.hinge0 && !this.beam.hinge1 ? (this.localPrimaryForceVector[1] = -o * i / (r * (4 + t)) * (t * e * e + 2 * i * (2 * e + s)), this.localPrimaryForceVector[4] = -o * s / (r * (4 + t)) * (t * e * e + 2 * (3 * e * e - s * s)), this.localPrimaryForceVector[5] = -2 * o * s * i / (e * e * (4 + t)) * (e + s)) : (this.localPrimaryForceVector[1] = -o / e * i, this.localPrimaryForceVector[4] = -o / e * s) : (this.localPrimaryForceVector[1] = -o * i / (r * (1 + t)) * (t * e * e + i * (3 * s + i)), this.localPrimaryForceVector[2] = o * s * i / (2 * e * e * (1 + t)) * (t * e + 2 * i), this.localPrimaryForceVector[4] = -o * s / (r * (1 + t)) * (t * e * e + s * (3 * i + s)), this.localPrimaryForceVector[5] = -o * s * i / (2 * e * e * (1 + t)) * (t * e + 2 * s))
	}, this.calculateContributionToInternalForces = function(e) {
		var t = this.beam.length,
			s = (this.beam.shearConstant, Math.cos(Math.PI - this.angle)),
			i = Math.sin(Math.PI - this.angle),
			e = t * e,
			t = t * this.position,
			s = this.size * s,
			i = this.size * i;
		return new Array(this.n(e, t, s), this.v(e, t, i), this.m(e, t, i))
	}, this.n = function(e, t, s) {
		s = e <= t ? 0 : -s;
		return s
	}, this.v = function(e, t, s) {
		s = e <= t ? 0 : -s;
		return s
	}, this.m = function(e, t, s) {
		t = e <= t ? 0 : -s * (e - t);
		return t
	}, this.calculateContributionToDisplacements = function(e) {
		var t = this.beam.length,
			s = Math.cos(Math.PI - this.angle),
			i = Math.sin(Math.PI - this.angle),
			e = t * e,
			t = t * this.position,
			s = this.size * s,
			i = this.size * i;
		return new Array(this.u(e, t, s), this.w(e, t, i), this.fi(e, t, i))
	}, this.u = function(e, t, s) {
		var i = this.beam.length;
		return (e < t ? s / i * e * (i - t) : s / i * t * (i - e)) / (this.beam.properties.modulusE * this.beam.properties.area)
	}, this.w = function(e, t, s) {
		var i = this.beam.length,
			r = this.beam.shearConstant,
			a = this.beam.properties.modulusE * this.beam.properties.secondAreaMoment,
			o = 0,
			o = e < t ? s / i * (2 * e * (e * e * (t - i) + t * (2 * i * i + t * (t - 3 * i))) + r * i * i * e * (i - t)) : s / i * (2 * t * (t * t * (e - i) + e * (2 * i * i + e * (e - 3 * i))) + r * i * i * t * (i - e));
		return o += this.localPrimaryForceVector[2] * generalDisplacementWMa(e, i), (o += this.localPrimaryForceVector[5] * generalDisplacementWMb(e, i)) / 12 / a
	}, this.fi = function(e, t, s) {
		return 0
	}
}

function BeamContinuousLoad(e, t, s, i, r, a) {
	this.beam = e, this.size = t, this.sizeChange = s, this.position = i, this.length = r, this.angle = a, this.localPrimaryForceVector = null, this.toString = function() {
		var e = "BeamContinuousLoad(";
		return e += "beam=" + this.beam.toString() + ",", e += "size=" + this.size.toString() + ",", e += "sizeChange=" + this.sizeChange.toString() + ")", e += "position=" + this.position.toString() + ",", e += "length=" + this.length.toString() + ",", e += "angle=" + this.angle.toString() + ","
	}, this.assemblePrimaryForceVector = function() {
		var e, t = this.beam.length,
			s = this.beam.shearConstant,
			i = Math.cos(Math.PI - this.angle),
			r = Math.sin(Math.PI - this.angle),
			a = t * this.position,
			o = a + t * this.length,
			n = this.size * i,
			c = this.size * r,
			l = (this.size + this.sizeChange) * i,
			h = (this.size + this.sizeChange) * r,
			i = a,
			r = t - o,
			o = o - a,
			a = t * t * t;
		this.localPrimaryForceVector = vectorAllocate(6), this.localPrimaryForceVector[0] = -o / (2 * t) * (r * (n + l) + o / 3 * (l + 2 * n)), this.localPrimaryForceVector[3] = -o / (2 * t) * (i * (n + l) + o / 3 * (n + 2 * l)), this.beam.hinge0 || this.beam.hinge1 ? !this.beam.hinge0 && this.beam.hinge1 ? (e = -o / (30 * a * (4 + s)) * (5 * s * (c * (i * i * (3 * i + 6 * r + 7 * o) + r * r * (3 * i + o) + o * o * (5 * i + 2 * r + o) + 8 * i * r * o) + h * (i * i * (3 * i + 6 * r + 8 * o) + r * r * (3 * i + 2 * o) + o * o * (7 * i + 4 * r + 2 * o) + 10 * i * r * o)) + 3 * (c * (10 * i * i * (2 * i + 3 * r + 4 * o) + o * o * (20 * i + 5 * r + 4 * o) + 20 * i * r * o) + h * (10 * i * i * (2 * i + 3 * r + 5 * o) + o * o * (40 * i + 15 * r + 11 * o) + 40 * i * r * o))), this.localPrimaryForceVector[1] = -e - (c + h) / 2 * o, this.localPrimaryForceVector[2] = e * t + o / 2 * (i * (c + h) + o / 3 * (c + 2 * h)), this.localPrimaryForceVector[4] = e) : this.beam.hinge0 && !this.beam.hinge1 ? (e = -o / (30 * a * (4 + s)) * (5 * s * (h * (r * r * (3 * r + 6 * i + 7 * o) + i * i * (3 * r + o) + o * o * (5 * r + 2 * i + o) + 8 * r * i * o) + c * (r * r * (3 * r + 6 * i + 8 * o) + i * i * (3 * r + 2 * o) + o * o * (7 * r + 4 * i + 2 * o) + 10 * r * i * o)) + 3 * (h * (10 * r * r * (2 * r + 3 * i + 4 * o) + o * o * (20 * r + 5 * i + 4 * o) + 20 * r * i * o) + c * (10 * r * r * (2 * r + 3 * i + 5 * o) + o * o * (40 * r + 15 * i + 11 * o) + 40 * r * i * o))), this.localPrimaryForceVector[1] = e, this.localPrimaryForceVector[4] = -e - (c + h) / 2 * o, this.localPrimaryForceVector[5] = -e * t - o / 2 * (r * (c + h) + o / 3 * (h + 2 * c))) : (this.localPrimaryForceVector[1] = -o / (2 * t) * (r * (c + h) + o / 3 * (h + 2 * c)), this.localPrimaryForceVector[4] = -o / (2 * t) * (i * (c + h) + o / 3 * (c + 2 * h))) : (e = -o / (60 * a * (1 + s)) * (10 * s * (c * (i * i * (3 * i + 6 * r + 7 * o) + r * r * (3 * i + o) + o * o * (5 * i + 2 * r + o) + 8 * i * r * o) + h * (i * i * (3 * i + 6 * r + 8 * o) + r * r * (3 * i + 2 * o) + o * o * (7 * i + 4 * r + 2 * o) + 10 * i * r * o)) + 3 * (c * (10 * i * i * (i + 3 * r + 3 * o) + o * o * (15 * i + 5 * r + 3 * o) + 20 * i * r * o) + h * (10 * i * i * (i + 3 * r + 3 * o) + o * o * (25 * i + 15 * r + 7 * o) + 40 * i * r * o))), md = o / (120 * t * t * (1 + s)) * (5 * s * (c * (i * i * (6 * r + 4 * o) + 2 * r * r * (3 * i + o) + o * o * (5 * i + 3 * r + o) + 12 * i * r * o) + h * (2 * i * i * (3 * r + o) + r * r * (6 * i + 4 * o) + o * o * (3 * i + 5 * r + o) + 12 * i * r * o)) + 2 * (c * (10 * i * i * (3 * r + 2 * o) + o * o * (10 * i + 5 * r + 2 * o) + 20 * i * r * o) + h * (10 * i * i * (3 * r + o) + o * o * (10 * i + 15 * r + 3 * o) + 40 * i * r * o))), this.localPrimaryForceVector[1] = -e - (c + h) / 2 * o, this.localPrimaryForceVector[2] = md + e * t + o / 2 * (i * (c + h) + o / 3 * (c + 2 * h)), this.localPrimaryForceVector[4] = e, this.localPrimaryForceVector[5] = -md)
	}, this.calculateContributionToInternalForces = function(e) {
		var t = this.beam.length,
			s = (this.beam.shearConstant, Math.cos(Math.PI - this.angle)),
			i = Math.sin(Math.PI - this.angle),
			r = t * e,
			a = t * this.position,
			o = a + t * this.length,
			e = this.size * s,
			t = this.size * i,
			s = (this.size + this.sizeChange) * s,
			i = (this.size + this.sizeChange) * i;
		return new Array(this.n(r, a, o, e, s), this.v(r, a, o, t, i), this.m(r, a, o, t, i))
	}, this.n = function(e, t, s, i, r) {
		t = e <= t ? 0 : e < s ? -.5 * (i + this.getQx(e, t, s, i, r)) * (e - t) : -.5 * (i + r) * (s - t);
		return t
	}, this.v = function(e, t, s, i, r) {
		t = e <= t ? 0 : e < s ? -.5 * (i + this.getQz(e, t, s, i, r)) * (e - t) : -.5 * (i + r) * (s - t);
		return t
	}, this.m = function(e, t, s, i, r) {
		var a, o;
		return 0 < Math.abs(i + r) ? (a = this.v(e, t, s, i, r), e <= t ? 0 : e < s ? a * (e - (e - t) * (i + 2 * (o = this.getQz(e, t, s, i, r))) / 3 / (i + o) - t) : a * (e - (s - t) * (i + 2 * r) / 3 / (i + r) - t)) : 0
	}, this.getQx = function(e, t, s, i, r) {
		i = !(e <= t) && e < s ? (e - t) * (r - i) / (s - t) + i : 0;
		return i
	}, this.getQz = function(e, t, s, i, r) {
		i = !(e <= t) && e < s ? (e - t) * (r - i) / (s - t) + i : 0;
		return i
	}, this.calculateContributionToDisplacements = function(e) {
		var t = this.beam.length,
			s = Math.cos(Math.PI - this.angle),
			i = Math.sin(Math.PI - this.angle),
			r = t * e,
			a = t * this.position,
			o = a + t * this.length,
			e = this.size * s,
			t = this.size * i,
			s = (this.size + this.sizeChange) * s,
			i = (this.size + this.sizeChange) * i;
		return new Array(this.u(r, a, o, e, s), this.w(r, a, o, t, i), this.fi(r, a, o, t, i))
	}, this.u = function(e, t, s, i, r) {
		var a = this.beam.length,
			o = i + r,
			n = s - t,
			r = e * e,
			s = t * t,
			c = n * n;
		return (e < t ? -n * e * (3 * t * o + n * (2 * o - i) - 3 * a * o) : e < t + n ? -1 / n * (s * t * a * (2 * i - o) + 3 * s * a * (n * i - e * (2 * i - o)) + 3 * t * e * (e * a * (2 * i - o) + n * (n * o - 2 * a * i)) - e * (r * a * (2 * i - o) - 3 * e * n * a * i + c * (n * (i - 2 * o) + 3 * a * o))) : n * (a - e) * (3 * t * o + n * (2 * o - i))) / 6 / a / (this.beam.properties.modulusE * this.beam.properties.area)
	}, this.w = function(e, t, s, i, r) {
		var a = this.beam.length,
			o = (this.beam.shearConstant, this.beam.properties.modulusE * this.beam.properties.secondAreaMoment),
			n = this.beam.properties.modulusG * this.beam.properties.shearArea,
			c = i + r,
			l = a - s,
			h = s - t,
			m = e * e,
			f = a * a,
			d = t * t,
			u = l * l,
			g = h * h,
			p = m * e,
			b = d * t,
			M = g * h,
			v = b * t,
			r = M * h,
			s = 0;
		return s = e < t ? (h * e * (10 * d * (3 * l * c + h * (i + c)) + 20 * t * (3 * u * c + l * h * (i + 4 * c) + g * (i + c)) + 20 * u * h * (2 * c - i) - 5 * l * (g * (2 * i - 7 * c) + 6 * c * m) + h * (g * (i + 7 * c) - 10 * m * (i + c))) / 360 / o + -h * e * (3 * t * c + h * (2 * c - i) - 3 * a * c) / 6 / n) / a : e < t + h ? (1 / h * (3 * (v * t) * a * (2 * i - c) + 15 * v * a * (h * i - e * (2 * i - c)) + 30 * b * e * (g * c - 2 * h * a * i + a * e * (2 * i - c)) - 30 * d * e * (M * (i - 2 * c) + 3 * g * a * c - 3 * h * a * i * e + a * m * (2 * i - c)) - 15 * t * e * (r * (2 * i - 3 * c) + 4 * M * a * (2 * c - i) - 6 * g * f * c + 4 * h * a * i * m + a * p * (c - 2 * i)) + e * (30 * l * g * c * (a * a - m) + 3 * (r * h) * (4 * c - 3 * i) + 15 * r * a * (2 * i - 3 * c) - 10 * M * (f * (2 * i - 7 * c) + m * (i + c)) - 30 * g * (f * a) * c + 15 * h * a * i * p + 3 * a * (p * e) * (c - 2 * i))) / 360 / o + -1 / h * (b * a * (2 * i - c) + 3 * d * a * (h * i - e * (2 * i - c)) + 3 * t * e * (e * a * (2 * i - c) + h * (h * c - 2 * a * i)) - e * (m * a * (2 * i - c) - 3 * e * h * a * i + g * (h * (i - 2 * c) + 3 * a * c))) / 6 / n) / a : (-h * (30 * b * c * (a - e) + 30 * d * h * (a - e) * (2 * c - i) + 15 * t * (e - a) * (g * (2 * i - 3 * c) + 6 * a * c * e) + 30 * l * c * e * (m - f) + 3 * M * (a - e) * (4 * c - 3 * i) + 10 * h * e * (f * (2 * i - 7 * c) + 3 * a * e * (2 * c - i) + m * (i + c)) + 30 * a * c * e * (f - m)) / 360 / o + h * (a - e) * (3 * t * c + h * (2 * c - i)) / 6 / n) / a, s += this.localPrimaryForceVector[2] * generalDisplacementWMa(e, a) / 12 / o, s += this.localPrimaryForceVector[5] * generalDisplacementWMb(e, a) / 12 / o
	}, this.fi = function(e, t, s, i, r) {
		return 0
	}
}

function BeamMomentLoad(e, t, s) {
	this.beam = e, this.size = t, this.position = s, this.localPrimaryForceVector = null, this.toString = function() {
		var e = "BeamMomentLoad(";
		return e += "beam=" + this.beam.toString() + ",", e += "size=" + this.size.toString() + ",", e += "position=" + this.position.toString() + ","
	}, this.assemblePrimaryForceVector = function() {
		var e = this.beam.length,
			t = this.beam.shearConstant,
			s = e * this.position,
			i = this.size,
			r = s,
			a = e - r,
			s = e * e * e;
		this.localPrimaryForceVector = vectorAllocate(6), this.beam.hinge0 || this.beam.hinge1 ? !this.beam.hinge0 && this.beam.hinge1 ? (this.localPrimaryForceVector[1] = -6 * i * r / (s * (4 + t)) * (r + 2 * a), this.localPrimaryForceVector[2] = -i / (e * e * (4 + t)) * (e * e * t - 2 * (e * e - 3 * a * a)), this.localPrimaryForceVector[4] = 6 * i * r / (s * (4 + t)) * (r + 2 * a)) : this.beam.hinge0 && !this.beam.hinge1 ? (this.localPrimaryForceVector[1] = -6 * i * a / (s * (4 + t)) * (a + 2 * r), this.localPrimaryForceVector[4] = 6 * i * a / (s * (4 + t)) * (a + 2 * r), this.localPrimaryForceVector[5] = -i / (e * e * (4 + t)) * (e * e * t - 2 * (e * e - 3 * r * r))) : (this.localPrimaryForceVector[1] = -i / e, this.localPrimaryForceVector[4] = i / e) : (this.localPrimaryForceVector[1] = -6 * i * r * a / (s * (1 + t)), this.localPrimaryForceVector[2] = i * a / (e * e * (1 + t)) * (2 * r - t * e - a), this.localPrimaryForceVector[4] = 6 * i * r * a / (s * (1 + t)), this.localPrimaryForceVector[5] = i * r / (e * e * (1 + t)) * (2 * a - t * e - r))
	}, this.calculateContributionToInternalForces = function(e) {
		var t = this.beam.length,
			e = (this.beam.shearConstant, t * e),
			t = t * this.position;
		return new Array(0, 0, this.m(e, t, this.size))
	}, this.m = function(e, t, s) {
		s = e <= t ? 0 : -s;
		return s
	}, this.calculateContributionToDisplacements = function(e) {
		var t = this.beam.length,
			e = t * e,
			t = t * this.position;
		return new Array(this.u(e, t, this.size), this.w(e, t, this.size), this.fi(e, t, this.size))
	}, this.u = function(e, t, s) {
		return 0
	}, this.w = function(e, t, s) {
		var i = this.beam.length,
			r = this.beam.properties.modulusE * this.beam.properties.secondAreaMoment;
		return ad = t, wd = e < ad ? s / i * e * 2 * (6 * ad * i - 3 * ad * ad - 2 * i * i - e * e) : s / i * 2 * (3 * ad * ad * i - e * (3 * ad * ad + 2 * i * i - e * (3 * i - e))), wd += this.localPrimaryForceVector[2] * generalDisplacementWMa(e, i), wd += this.localPrimaryForceVector[5] * generalDisplacementWMb(e, i), wd / 12 / r
	}, this.fi = function(e, t, s) {
		return 0
	}
}

function generateHTML(e) {
	if (!(e instanceof Model)) throw new IllegalArgumentException;
	for (var t = "<TABLE border='1'>", s = 0; s < e.nodes.length; s++) t += generateNodeResult(e.nodes[s], e);
	return t += "</TABLE>"
}

function generateNodeResult(e, t) {
	var s = "<TR><TD>NODE</TD><TD>" + t.nodes.indexOf(e) + "</TD></TR>";
	s += "<TR><TD></TD><TD>coordX</TD><TD>" + e.x + "</TD></TR>", s += "<TR><TD></TD><TD>coordZ</TD><TD>" + e.z + "</TD></TR>", s += "<TR><TD></TD><TD>restraintX</TD><TD>" + e.restraints[0] + "</TD></TR>", s += "<TR><TD></TD><TD>restraintZ</TD><TD>" + e.restraints[1] + "</TD></TR>", s += "<TR><TD></TD><TD>restraintR</TD><TD>" + e.restraints[2] + "</TD></TR>";
	var i = t.assembleTotalNodeDisplacementVector(e);
	s += "<TR><TD></TD><TD>u</TD><TD>" + i[0] + "</TD></TR>", s += "<TR><TD></TD><TD>w</TD><TD>" + i[1] + "</TD></TR>", s += "<TR><TD></TD><TD>fi</TD><TD>" + i[2] + "</TD></TR>";
	e = t.assembleNodeReactionVector(e);
	return s += "<TR><TD></TD><TD>reactionX</TD><TD>" + e[0] + "</TD></TR>", s += "<TR><TD></TD><TD>reactionZ</TD><TD>" + e[1] + "</TD></TR>", s += "<TR><TD></TD><TD>reactionM</TD><TD>" + e[2] + "</TD></TR>"
}

function preCheck(e) {
	for (var t = new Array(0, 0, 0), s = 0; s < e.nodes.length; s++)
		for (var i = 0; i < 3; i++) t[i] += e.nodes[s].restraints[i];
	console.log("restraintCounts=" + t);
	for (var r = t[0] + t[1] + t[2], a = 0, s = 0; s < e.beams.length; s++) e.beams[s].hinge0 && a++, e.beams[s].hinge1 && a++;
	console.log("hingeCount=" + a);
	for (var o = 0, s = 0; s < e.nodes.length; s++) e.isNodeRotationFree(e.nodes[s]) && o++;
	console.log("rotationFreeNodeCount=" + o), 0 == t[0] && solver_log("warning", "MODEL_NOT_RESTRAINED_X"), 0 == t[1] && solver_log("warning", "MODEL_NOT_RESTRAINED_Z"), r < 3 && solver_log("error", "MODEL_NOT_ENOUGH_RESTRAINTS"), 0 < a - o && r - a + o < 3 && solver_log("warning", "MODEL_PROBABLY_NOT_ENOUGH_RESTRAINTS")
}

function checkProperties(e) {
	for (var t = 0; t < e.beams.length; t++) {
		var s = e.beams[t].properties;
		s.height < 0 ? solver_log("error", "BEAM_ILLEGAL_HEIGHT", t) : 0 == s.height ? solver_log("warning", "BEAM_ZERO_HEIGHT", t) : s.area < 0 ? solver_log("error", "BEAM_ILLEGAL_AREA", t) : 0 == s.area ? solver_log("warning", "BEAM_ZERO_AREA", t) : s.shearArea < 0 ? solver_log("error", "BEAM_ILLEGAL_SHEAR_AREA", t) : 0 == s.shearArea ? solver_log("warning", "BEAM_ZERO_SHEAR_AREA", t) : s.secondAreaMoment < 0 ? solver_log("error", "BEAM_ILLEGAL_SECOND_MOMENT", t) : 0 == s.secondAreaMoment ? solver_log("warning", "BEAM_ZERO_SECOND_MOMENT", t) : s.modulusE < 0 ? solver_log("error", "BEAM_ILLEGAL_ELASTICITY", t) : 0 == s.modulusE ? solver_log("warning", "BEAM_ZERO_ELASTICITY", t) : s.modulusG < 0 ? solver_log("error", "BEAM_ILLEGAL_SHEAR_ELASTICITY", t) : 0 == s.modulusG ? solver_log("warning", "BEAM_ZERO_SHEAR_ELASTICITY", t) : s.transversalContractionCoef < 0 ? solver_log("error", "BEAM_ILLEGAL_TRANS_COEF", t) : s.thermalExpansionCoef < 0 && solver_log("error", "BEAM_ILLEGAL_THERM_COEF", t)
	}
}

function postCheck(e) {
	for (var t = !1, s = 0, i = 0, r = 0; r < e.secondaryDisplacementVector.length; r++) {
		var a = e.secondaryDisplacementVector[r];
		a != a && (t = !0), r % 3 == 2 ? Math.abs(i) < Math.abs(a) && (i = a) : Math.abs(s) < Math.abs(a) && (s = a)
	}
	console.log("isThereNaN=" + t), console.log("extremeTranslation=" + s), console.log("extremeRotation=" + i), t && solver_log("error", "MODEL_UNSOLVABLE"), 100 < Math.abs(s) ? solver_log("error", "EXTREME_TRANSLATION", s) : .1 < Math.abs(s) && solver_log("warning", "EXTREME_TRANSLATION", s), .7 < Math.abs(i) ? solver_log("error", "EXTREME_ROTATION", i) : .1 < Math.abs(i) && solver_log("warning", "EXTREME_ROTATION", i)
}

function NodeDeformationLoad(e, t) {
	this.node = e, this.components = t, this.toString = function() {
		var e = "NodeDeformationLoad(";
		return e += "node=" + this.node.toString() + ",", e += "componentX=" + this.components[0].toString() + ",", e += "componentZ=" + this.components[1].toString() + ",", e += "componentR=" + this.components[2].toString() + ","
	}
}